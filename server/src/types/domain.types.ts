import type { AIGeneration, Document, Profile, Subscription, User } from '@prisma/client';

export type UserEntity = User;
export type ProfileEntity = Profile;
export type DocumentEntity = Document;
export type AIGenerationEntity = AIGeneration;
export type SubscriptionEntity = Subscription;
